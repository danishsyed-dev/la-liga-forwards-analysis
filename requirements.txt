# Core dependencies (needed to run app.py and generate_static.py)
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
streamlit>=1.28.0
plotly>=5.18.0

# Data pipeline — only needed if running scripts/ pipeline
# Install with: pip install -r requirements-scraping.txt
# Or: pip install ".[scraping]"
